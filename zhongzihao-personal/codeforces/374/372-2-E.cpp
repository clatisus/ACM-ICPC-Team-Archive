#include<bits/stdc++.h>
const int N = 100010;
using namespace std;
deque <int> q; 
int L, n, p, t, l[N], r[N], f[i];

int main()
{
	scanf("%d%d%d%d", &L, &n, &p, &t);
	for (int i = 0; i < n; i++)
		scanf("%d%d", &l[i], &r[i]);
	q.push_back(0);
	for (int i = 0; i < n; i++)
	{
		
	}
}
